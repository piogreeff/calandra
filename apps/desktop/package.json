{
  "name": "@calandra/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "echo desktop Tauri shell available via pnpm --filter @calandra/desktop dev:tauri",
    "dev:tauri": "tauri dev",
    "tauri": "tauri",
    "build": "tsc --noEmit",
    "build:tauri": "tauri build",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@calandra/contract": "workspace:*",
    "@calandra/parser": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3"
  }
}
